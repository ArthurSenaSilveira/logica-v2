
        let n1 = Number(5)
        let n2 = Number(5)
        let res = (n1) + (n2)
        let sub = (n1) - (n2)
        let mult = (n1) * (n2)
        let div = (n1) / (n2)
        
        

        console.log(`O resultado da soma de ${n1} com ${n2} é ${res}`)
        console.log(`O resultado da subtração de ${n1} com ${n2} é ${sub}`)
        console.log(`O resultado da multiplicação de ${n1} com ${n2} é ${mult}`)
        console.log(`O resultado da divisão de ${n1} com ${n2} é ${div}`)
      
      //(`Olá, a soma de ${n1} com ${n2} é ${n1 + n2}`)
