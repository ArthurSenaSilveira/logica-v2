let nomeDoUsuario = 'Arthur'
let idadeDoUsuario = 17
let estadoDoUsuario = 'SP'

let usuario = [
  `Nome: ${nomeDoUsuario}
            Idade: ${idadeDoUsuario}
            Estado: ${estadoDoUsuario}`
]
console.log(usuario)

/*   let nomeDoUsuario = prompt(`Digite seu nome`)
      let nomes = [
        ' Arthur',
        'guilherme',
        'Ana Julia',
        'Edna',
        'Edivaldo',
        'eva',
        'Hélio'
      ] // sintaxe literal

      // let nomes = new Array("João", " Maria", " Arthur") // função construtora

      nomes[6] = `${nomeDoUsuario}`
      for (let i = 0; i < nomes.length; i++) {
        console.log(`Nome: ${nomes.sort()[i]}`)
      }
      console.log(`Na lista existem ${nomes.length + 1} nomes`) */
