let números = [2, 5, 6, 8, 4, 5, 1, 1, 0, 100, 200, 300]
      let números2 = []

      for(let i = 0; i < números.length; i++){
        let numeros = (números[i] * 2)

        números2.push(numeros)
      }

      console.log(números2)