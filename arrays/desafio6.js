const nomes = [
    ' Arthur',
    'guilherme',
    'Ana Julia',
    'Edna',
    'Edivaldo',
    'eva',
    'Hélio'
  ]
  for(let i = nomes.length - 1; i >= 0; i--) { 
    console.log(`${nomes[i]} <hr>`)
  }
